<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/glitch.css">
  <link rel="stylesheet" href="/styles/main.css">
  <title>{{ title }}</title>
</head>
<body>
  <div class="container">
    <h1>WERQSHOP Talk</h1>
    <h2>{{ title }}</h2>
    <h3>Speaker: 
      {% if website %}
        <a href="{{ website }}">{{ speaker }}</a> {% if affiliation %}({{ affiliation }}){% endif %}
      {% else %}
        {{ speaker }} {% if affiliation %}({{ affiliation }}){% endif %}
      {% endif %}
    </h3>
    <div class="speaker-info">
      {% if headshot %}
        <img src="{{ headshot }}" alt="{{ speaker }}" class="{{ speaker }} headshot" />
      {% endif %}
      {% if bio %}
        <p class="speaker-bio">{{ bio }}</p>
      {% endif %}
    </div>
    {# {% if headshot %}<img src="{{ headshot }}" alt="{{ speaker }}" />{% endif %}
    {% if bio %}<p>{{ bio }}</p>{% endif %} #}
    <div class="talk-abstract">
      <h3>Abstract</h3>
      {{ content | safe }}
    </div>
    {% if slides %}<p><a href="/talks/slides/{{ slides }}">Slides</a></p>{% endif %}
    <p><a href="/">Back to home</a></p>
  </div>
</body>
</html>
